- defaults:
      link:
          create: true
          relink: true

- clean: ["~"]

- create:
      - ~/code

- link:
      ~/.dotfiles: ""
      ~/.vimrc:
      ~/.zshrc:

- shell:
      - git submodule update --init --recursive
