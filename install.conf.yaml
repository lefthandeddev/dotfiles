- defaults:
    link:
      create: true
      relink: true

- clean: ["~"]

- create:
    - ~/code

- link:
    ~/.dotfiles: ""
    ~/.gitconfig:
    ~/.vimrc:
    ~/.zshrc:

- shell:
    - git submodule update --init --recursive
